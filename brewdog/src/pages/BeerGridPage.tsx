import BeerGrid from "../components/BeerGrid/BeerGrid";

const BeerGridPage = (): JSX.Element => (
  <div className="container">
    <h1>Beer Grid</h1>
    <BeerGrid />
  </div>
);

export default BeerGridPage;
